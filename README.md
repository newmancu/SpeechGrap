# SpeechGrap

SpeechGrap — полностью локальное приложение для преобразования речи в текст с возможностью быстрой вставки результата через буфер обмена.


# 🚀 Feauter

- 🎤 Запись аудио с микрофона в реальном времени

- 🔊 Преобразование речи в текст с использованием локальных моделей

- 📋 Автоматическое сохранение результата в буфер обмена

- ⌨️ Горячие клавиши для удобного управления

- 🔒 Полная локальность — данные не покидают ваше устройство

# 📦 Installation

## Utils
```bash
sudo apt-get install xsel
```

## Main App

1) Установите [uv](https://docs.astral.sh/uv/getting-started/installation/)
2) Установите зависимости:
```bash
uv sync
```
3) Активируйте виртуальную среду:
   1) bash/zsh: `source .venv/bin/activate`
   2) sh: `. .venv/bin/activate`
4) Настройте окружение:
```bash
cp .env.example .env
```
5) Запустите приложение:
```bash
sudo python3 main.py
```
## Notification service

### Gnome (Ubuntu)

1) Установите Rust (если не установлен)
2) Перейдите в директорию проекта с сервисом уведомлений:
```bash
cd notifier/linux_display
```
2) Настройте сервис:
```bash
export DN_BASE=deploy/linux_display.service && cp "$DN_BASE.example" "$DN_BASE" && sed -i "s/\$USER/$(echo "$USER" | sed 's/[\/&]/\\&/g')/g" "$DN_BASE"
```
3) Соберите и установите:
```bash
cargo build --release
sudo ./install
```

# Usage
1) Запустите приложение

2) Для начала записи используйте сочетание клавиш **SG_HOTKEY** (по умолчанию Ctrl+Alt+R)

3) Говорите в микрофон

4) Для завершения записи снова нажмите **SG_HOTKEY**

5) Распознанный текст автоматически сохранится в буфер обмена

6) Вставьте текст в любое текстовое поле с помощью Ctrl+V

## ⚙️ Setup

Настройки приложения находятся в файле .env. Вы можете изменить:

- Сочетание клавиш для активации записи
- Параметры аудиозаписи
- Настройки модели распознавания речи

## 🛠️ Dev features
- Полностью локальная обработка аудио

- Интеграция с системными уведомлениями

- Автоматическое добавление текста в буфер обмена


### Dev utils

 - Загрузить .env файл:
```bash
export $(grep -v '^#' .env | sed -E 's/\s*=\s*/=/' | grep -v '^$')
```